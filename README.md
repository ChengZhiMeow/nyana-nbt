### Gradle

如果需要使用此库，请使用以下依赖配置。

```kotlin
repositories {
    maven("https://repo.catnies.top/releases/")
}
```
```kotlin
dependencies {
    implementation("net.nyana:nyana-nbt-tag:1.0.0")  // Tag Module
    implementation("net.nyana:nyana-nbt-codec:0.15")  // for DFU 8.0+
}
```